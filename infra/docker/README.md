# Infra Docker

Standard compose path:
1. `infra/docker/docker-compose.yml`

Compatibility path still available:
1. `infra/docker-compose.yml`

During transition both files are kept to avoid breaking existing scripts/docs.
